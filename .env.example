# Environment variables for PDF generation

# For Vercel production (automatically set via vercel.json)
CHROMIUM_REMOTE_EXEC_PATH=https://github.com/Sparticuz/chromium/releases/download/v131.0.0/chromium-v131.0.0-pack.tar

# For local development (optional - will use system Chrome if not set)
# CHROMIUM_LOCAL_EXEC_PATH=/usr/bin/google-chrome

# Google Analytics 4 Configuration
# Get this from Google Analytics 4 dashboard: Admin -> Data Streams -> Web -> Measurement ID
NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# Supabase configuration
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Email Configuration (for analytics reports)
SMTP_HOST=smtp.gmail.com
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# Cron Job Security (generate a random secure token)
CRON_SECRET=your_secure_cron_token_here

# Vercel environment (set automatically on Vercel)
# VERCEL_URL=your-app.vercel.app