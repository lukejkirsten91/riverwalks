# Staging Environment Variables for Riverwalks
# Copy this to your Vercel staging deployment environment variables

# Environment Identification
NODE_ENV=production
NEXT_PUBLIC_ENVIRONMENT=staging

# Supabase Configuration (Use staging/development instance)
# NEXT_PUBLIC_SUPABASE_URL=your_staging_supabase_url
# NEXT_PUBLIC_SUPABASE_ANON_KEY=your_staging_supabase_anon_key
# SUPABASE_SERVICE_ROLE_KEY=your_staging_service_role_key

# Stripe Configuration (Use test mode in staging)
STRIPE_LIVE_MODE=false
# NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_your_test_publishable_key
# STRIPE_SECRET_KEY=sk_test_your_test_secret_key
# STRIPE_WEBHOOK_SECRET=whsec_your_staging_webhook_secret

# Email Configuration (Optional for staging)
# SMTP_HOST=smtp.gmail.com
# SMTP_USER=staging@riverwalks.co.uk
# SMTP_PASS=your_staging_app_password

# Google Analytics (Use separate GA4 property for staging)
# NEXT_PUBLIC_GA_MEASUREMENT_ID=G-STAGING_ID

# Cron Job Security
# CRON_SECRET=your_staging_cron_token

# PDF Generation
CHROMIUM_REMOTE_EXEC_PATH=https://github.com/Sparticuz/chromium/releases/download/v131.0.0/chromium-v131.0.0-pack.tar

# Vercel URL (automatically set)
# VERCEL_URL=riverwalks-staging.vercel.app